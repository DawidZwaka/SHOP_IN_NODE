extends ../layouts/adminLayout.pug
include ../partials/admin/mixins/card
include ../partials/mixins/modal

block content
    |
    .grid.slides      
        each image in images
            .grid__col
                +card(slide, "slide")
        .grid__col
            +addItemCard("/admin/add-slide")
    +simpleDeleteModal("slide")