extends ../layouts/adminLayout.pug
include ../partials/admin/mixins/user
include ../partials/mixins/card
include ../partials/mixins/modal

block content
    |
    .grid.grid--small
        each user in users
            .grid__col
                +userCard(user)
        .grid__col
            +addItemCard("/admin/add-user", "addItemCard--small")
    |
    +simpleDeleteModal("product")